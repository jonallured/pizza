{
  "name": "pizza",
  "version": "0.1.0",
  "description": "Jon's personal pi configuration — skills, extensions, prompts, and themes",
  "keywords": [
    "pi-package"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run --reporter=dot",
    "typecheck": "tsc --noEmit"
  },
  "pi": {
    "extensions": [
      "extensions/print-session-id.ts"
    ],
    "skills": [
      "skills"
    ],
    "prompts": [
      "prompts"
    ],
    "themes": [
      "themes"
    ]
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "^0.62.0",
    "prettier": "^3.8.1",
    "typescript": "^6.0.2",
    "vitest": "^3.2.4"
  }
}
